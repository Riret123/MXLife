Working file: `MXLife_screening_and_extraction_templates.xlsx`

- `papers_screening` - one row per paper, include/exclude decision and why.
- `raw_extractions` - one row per data point, exactly as reported in the source paper. No unit conversion or normalization here, that happens in `data/processed`.

Don't rewrite old rows without leaving a note in the `notes` column - every number needs to trace back to a specific paper/figure/table.
